# ========================================
# PLANTILLA DE CONFIGURACIÓN DEL BACKEND
# ========================================
#
# INSTRUCCIONES:
# 1. Copia este archivo y renómbralo a ".env"
# 2. Reemplaza todos los valores de ejemplo con tus credenciales reales
# 3. NUNCA subas el archivo .env a Git (debe estar en .gitignore)
#
# ========================================

# Configuración del entorno
# Valores: development, production, test
NODE_ENV=development

# ========================================
# SUPABASE CONFIGURATION
# ========================================
# Obtén estas credenciales desde tu proyecto en https://supabase.com
# Panel de administración > Settings > API

# URL de tu proyecto Supabase
SUPABASE_URL=https://tu-proyecto.supabase.co

# Clave pública anónima (segura para usar en el cliente)
SUPABASE_ANON_KEY=tu-supabase-anon-key-aqui

# Clave de servicio (PRIVADA - solo para servidor)
# ADVERTENCIA: Esta clave tiene acceso completo a la base de datos
SUPABASE_SERVICE_ROLE=tu-supabase-service-role-key-aqui

# ========================================
# JWT AUTHENTICATION
# ========================================
# Clave secreta para firmar tokens JWT
#
# IMPORTANTE:
# - Debe ser un string aleatorio de al menos 64 caracteres
# - Genera uno seguro con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# - NUNCA uses valores por defecto o de ejemplo en producción
# - Si cambias esta clave, todos los tokens existentes quedarán invalidados
#
JWT_SECRET=genera-un-secreto-aleatorio-muy-largo-y-seguro-aqui

# ========================================
# CORS CONFIGURATION
# ========================================
# Configuración de seguridad para Cross-Origin Resource Sharing
#
# IMPORTANTE:
# - Lista de dominios permitidos separados por comas (sin espacios)
# - En DESARROLLO: usa localhost con los puertos de tu frontend
# - En PRODUCCIÓN: SOLO lista tus dominios reales (https://tudominio.com)
# - NUNCA uses "*" o dejes esto vacío en producción
#
# Ejemplos:
# Desarrollo: http://localhost:3000,http://localhost:3001
# Producción: https://tudominio.com,https://www.tudominio.com
#
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000

# ========================================
# SERVER CONFIGURATION
# ========================================

# Puerto del servidor backend (por defecto: 4000)
PORT=4000

# Nivel de logging (por defecto: info)
# Valores: error, warn, info, debug
# LOG_LEVEL=info
